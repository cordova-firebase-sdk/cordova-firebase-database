{
  "name": "cordova-firebase-database",
  "version": "0.0.3",
  "description": "",
  "cordova": {
    "id": "cordova-firebase-database",
    "platforms": [
      "android",
      "ios",
      "browser"
    ]
  },
  "scripts": {
    "eslint": "node node_modules/eslint/bin/eslint www && node node_modules/eslint/bin/eslint src",
    "test:android": "cordova-paramedic --cleanUpAfterRun --verbose --platform android --plugin . --useTunnel",
    "test:browser": "cordova-paramedic --cleanUpAfterRun --verbose --platform browser --plugin . --useTunnel",
    "build": "npm run tslint && rm -rf dist && npx tsc && cp -R package.json plugin.xml README.md src dist/",
    "test": "npm run jest",
    "tslint": "npx tslint --project .",
    "jest": "npm run tslint && npx jest",
    "jest:watch": "npx jest --watch",
    "tsdoc": "npx typedoc --options typedoc.js src/ts "
  },
  "keywords": [
    "ecosystem:cordova",
    "cordova-android",
    "cordova-ios",
    "cordova-browser"
  ],
  "author": "Masashi Katsumata<wf9a5m75@gmail.com>",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/cordova": "0.0.34",
    "@types/es6-promise": "^3.3.0",
    "@types/jest": "^23.3.7",
    "cordova-js": "^4.2.4",
    "cordova-paramedic": "git+https://github.com/apache/cordova-paramedic.git",
    "es6-promise": "^4.2.5",
    "eslint": "^3.19.0",
    "jest": "^23.6.0",
    "request": "^2.88.0",
    "ts-jest": "^23.10.4",
    "tslint": "^5.11.0",
    "typedoc": "^0.13.0",
    "typescript": "^3.1.3"
  },
  "dependencies": {
    "cordova-firebase-core": "file:../cordova-firebase-core/dist"
  }
}
